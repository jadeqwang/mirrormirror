# (untracked)

Eval photographs go here, named to match the `image` field in `../cases.json`.

Nothing in this directory should be committed. See `../README.md` for sourcing rules —
briefly: real photos only, consent required, no staged protected-category costumes, and
the piece's wall label promises no visitor images are retained, so a repo full of them is
a promise you have to explain.

Add to `.gitignore`:

    fixtures/eval/images/*
    !fixtures/eval/images/README.md
